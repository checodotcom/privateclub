---
layout: base.njk
---
<div class="window">
  <div class="window-titlebar">
    <span>📝 {{ title }}</span>
    <div class="titlebar-controls">
      <span class="titlebar-btn">_</span>
      <span class="titlebar-btn">□</span>
      <a href="/posts/" class="titlebar-btn">✕</a>
    </div>
  </div>
  <div class="window-content">
    <div class="post-header">
      {% if image %}
        <img class="post-cover" src="{{ image }}" alt="" />
      {% endif %}
      <h1 class="post-title">{{ title }}</h1>
      <div class="post-meta">
        <span>📅 {{ date | dateFormat }}</span>
        {% if tags %}
          <span>
            🏷️
            {% for tag in tags %}
              {% if tag != "posts" %}
                <a class="tag" href="/tags/{{ tag }}/">{{ tag }}</a>
              {% endif %}
            {% endfor %}
          </span>
        {% endif %}
      </div>
    </div>

    {% set seleccionEmbed = seleccionUrl | spotifyEmbedUrl %}
    {% if categoria == "selecciones" and seleccionEmbed %}
      <div class="inset post-body" style="margin-bottom:12px;">
        <h2>La selección</h2>
        <iframe style="border-radius:12px" src="{{ seleccionEmbed }}" width="100%" height="352" frameBorder="0" allowfullscreen="" allow="autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture" loading="lazy"></iframe>
      </div>
    {% endif %}

    <div class="inset post-body">
      {{ content | safe }}
    </div>

    <div style="margin-top:12px;display:flex;gap:8px;">
      <a href="/posts/" class="btn">← Back to Posts</a>
      <a href="/" class="btn">🏠 Home</a>
    </div>
  </div>
</div>
